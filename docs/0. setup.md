# Testing in local ICP network

> Requires `dfx 0.20+`, `cargo` nightly, and `candid-extractor` in `$PATH`.

---

## Start a local network

```bash
dfx start --background
```

Run the different local tests agains your ICP local network as described in this folder `docs/`.
